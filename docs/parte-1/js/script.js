const startForm = {
    // UI DECLARATION
    ui: {
    },

    // FUNCTIONS
    functions: {
        formValidation: () => {
        }

    },

    //EVENTS
    events: {
        init: () => {
        }
    }
};

//INIT FUNCTION
startForm.events.init();